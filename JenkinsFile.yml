pipeline {
    agent any
    stages {
        stage('Clonage du dépôt GitHub') {
            steps {
                git branch: ‘main’, url : 'https://github.com/ghoutibkl/OSDetector.git'
            }
        }
    }
}